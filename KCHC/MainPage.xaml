<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="KCHC.MainPage">
    <Grid VerticalOptions="CenterAndExpand">
        <Image Source="kchc.ico" HeightRequest="300" Aspect="AspectFill"/>
        <CarouselView ItemsSource="{Binding Artists}" HorizontalOptions="FillAndExpand" VerticalOptions="CenterAndExpand">
            <CarouselView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Padding="10" VerticalOptions="CenterAndExpand">
                        <Image Source="{Binding PhotoPath}" HeightRequest="300" Aspect="AspectFill" VerticalOptions="CenterAndExpand"/>
                        <Label Text="{Binding Name}" FontAttributes="Bold" FontSize="24" TextColor="White" HorizontalOptions="Center"/>
                        <Label Text="{Binding Description}" FontSize="18" TextColor="White" HorizontalOptions="Center"/>
                    </StackLayout>
                </DataTemplate>
            </CarouselView.ItemTemplate>
        </CarouselView>
    </Grid>
</ContentPage>
